# Farming v2.2.45

[English](./v2.2.45.md)

里程碑代号：**STRIPED**

本次发布把慢 Browser Scenario 分散到可用 Runner，同时保留单 Worker 隔离和已验证的 npm 发布路径。

## 耗时均衡验证

- CI 发现精确 Playwright Test Location，并把相邻 Location Group 条纹分散到 9 个 Chromium Job，避免一个产品领域独占慢连续 Shard。
- 每个 Chromium Job 仍然只有一个 Worker；共享同一源码位置的 Generated Test 保持原子分组。
- npm Publish 保留 OIDC Trusted Publishing 必需的新 npm CLI，同时关闭大型 Package Content Notice 清单。
- Release Stage 继续在 Exact-SHA CI 尾段同时准备已验证资产。

## 升级

```bash
npm install --global farming-code@2.2.45 && farming daemon
```
