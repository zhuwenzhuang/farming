# Farming v2.2.12

[English](https://github.com/zhuwenzhuang/farming/blob/main/release-notes/v2.2.12.md)

里程碑代号：**PROJECTS AUX**

这个版本调整 Farming Code 的 Project 导航、Workspace 选择、Chat 输入区、Project Files 和本地 Token 活动展示，并增加 Project / Codex 生命周期操作、可安装桌面应用引导和公开产品导览。

## Highlights

- **Project 置顶顺序与操作菜单**：可在 `...` 菜单中置顶或取消置顶 Project；多个置顶项按加入顺序排列。菜单同时提供在 Host 文件管理器中显示、创建永久 Git Worktree、重命名、全部标为已读、归档聊天、从 Farming 移除，以及对符合条件的 Worktree 执行显式危险删除流程。
- **每个 Project 独立控制 Agent 可见性**：Project 行新增紧凑的眼睛控件，只隐藏或恢复该 Project 的 Agent 列表；Files 继续可用，Project 标题与操作按钮保持稳定对齐。
- **原地逐步展开 Workspace 目录树**：New Agent 的 Host 目录选择器改为按需原地展开，不再逐级替换当前层。选择深层 Workspace 时会保留父级和兄弟目录上下文，远程浏览器同样选择服务端路径。
- **本地 Token 活动详情**：Usage 可展开为 24 小时和 52 周视图，展示缩写 Token 总量、整点标签、选中日期读数、最近七天与此前七天对比、峰值日，以及按 Agent 类型聚合的分时直方图。Token King 使用皇冠轮廓，超过 1B Token 的日期使用火焰轮廓，同时保留原有热度颜色。
- **Chat 与 Terminal 输入区控制一致**：两种 Runtime 都使用输入区边缘悬停出现的收起 / 恢复操作。收起只隐藏输入区，保留未发送草稿和当前 Transcript / Terminal；Chat 末尾间距和复制按钮也进一步收紧，让最新结果更靠近输入框。
- **Project Files 布局与语法诊断**：Files、Changes、Untracked、History 和目录树使用同一套字体、行高与层级间距。Markdown 链接会保留返回来源 Agent 的路径；JavaScript / TypeScript 编辑提供语法诊断，不执行 Project Typecheck。
- **History 分页与 Codex 归档顺序**：History 只在页边界继续加载更早的 Provider Session。归档实时 Codex Agent 时先完成 Farming 本地生命周期，再对稳定 Session 执行 Provider Archive；Provider 失败会显式报告，但不会重新唤起 Agent。
- **可安装桌面应用引导**：Farming Code 提供应用 / 全屏入口和 Chrome 独立窗口安装说明。安装后的启动地址只包含 Base Path，并复用现有鉴权 Cookie，不会把启动 Token 固化到 URL。
- **产品导览与响应式布局更新**：公开 Code / CRT 截图更新为当前界面，并补充界面切换路径；Transcript 字体和宽窄布局减少了空白与跳动。
- **浏览器测试改用语义与 Test ID 契约**：Project 操作、Terminal 链接、Context Menu、Files、Chat 连续性、Workspace 选择、Usage 和平台差异改用语义或 Test ID 契约验证。

## Verification

- 184 项 Backend 与源码契约测试
- TypeScript typecheck 与 ESLint
- Project 操作、Workspace 目录展开、Chat 间距、Project Files、编辑器诊断、Agent 返回和 Usage 活动的定向 Chromium Playwright 覆盖
- 真实 Codex 跨 Skin 发布门禁，覆盖 Code Terminal、ACP Chat、CRT、模型控制和 Resize 恢复
- 确定性 Terminal 输入门禁，Key-to-PTY-output p95 在 250 ms 发布上限内
- `/farming` 生产构建与 Mac 浏览器 Smoke
- npm 包全局安装、Daemon 启动、鉴权状态和原生 `node-pty` Smoke
- 原生 macOS CLI 与独立 App Bundle 校验和启动 Smoke
- 远端 Linux Token 鉴权、Native PTY Smoke 与进程清理检查
- 对待推送文本历史执行私有路径、内部链接、Token 和密钥材料扫描

## 兼容性说明

- macOS CLI 产物使用 ad-hoc 签名，尚未进行 Developer ID 签名或公证。根据本机安全设置，首次运行可能需要先显式手动允许，再重新执行 CLI。

## 升级

```bash
npm install --global farming-code@2.2.12 && farming daemon
```
