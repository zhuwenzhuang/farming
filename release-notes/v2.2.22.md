# Farming v2.2.22

[简体中文](./v2.2.22.zh_cn.md)

This release closes race windows around Agent lifecycle, structured Chat, Project Files, settings, and updates while preserving upgrade recovery for older ACP Sessions.

## Fixed

- Serializes ACP prompt, steer, cancellation, and queued-message admission per Agent. Definite turn-end steer failures preserve message order, ambiguous failures are never replayed, and messages submitted during cancellation start at most once after the old Turn reaches a terminal state.
- Gives every observed ACP child Session an isolated control owner. Duplicate stops join one provider request, parent cancellation includes children proven active, stale bindings cannot modify replacements, and late updates cannot reopen terminal child state.
- Restores ACP Chat automatically from metadata created before process-group identities existed. Upgrade recovery loads the exact saved Provider Session without per-Session operator confirmation, while every newly started ACP process persists the precise identity used by later cleanup.
- Journals Agent Create, Update, Delete, and Archive intent before external side effects, then recovers unfinished operations without launching duplicate runtimes. Frequently updated Agent state is stored separately from lifecycle and process ownership metadata so hot writes cannot overwrite safety-critical identity.
- Fences Project Files saves, Create, Rename, Move, and Delete across browser and backend owners. Late UI responses cannot steal newer state, uncertain mutations reconcile from authoritative filesystem reads without automatic replay, and newer drafts survive older save completions.
- Preserves exact ACP Fork state and provider identity across Codex and process-owned Claude forks, including bounded child transcripts, patch decisions, startup rollback, and failed-cleanup reporting.
- Bounds main-page Session and Agent Home requests, serializes CRT settings saves, and reconciles uncertain responses from authoritative settings. Daemon process identity is locale-stable across restarts.
- Starts packaged daemons directly under the recorded Server PID, so Linux stop and release smoke verify the actual listener instead of a shell wrapper.

## Changed

- npm and Bundle updates now prepare into staging while the current server remains available. Applying a prepared update requires a separate restart action, and a failed npm directory switch restores the previous package before restart.
- Codex collaboration activity now appears as a compact named `updated` / `finished` timeline in Chat. Selecting an event expands and focuses its exact Process evidence without presenting internal collaboration events as separate writable Agents.

## Verification

- 198 backend and source-contract tests, TypeScript typecheck, and ESLint
- 17 focused Chromium ACP flows and the iPhone WebKit send/interrupt flow
- Default-configuration legacy and current ACP restart/recovery through Computer Use, including post-recovery send and interrupt
- Remote Terminal chaos with two viewers, viewport churn, throttling, offline recovery, reload, and exact post-recovery input
- Real Codex cross-skin release case, terminal-input latency gate, production build, npm package smoke, standalone CLI smoke, and dependency audit

## Upgrade

```bash
npm install --global farming-code@2.2.22 && farming daemon
```
