// Generated by scripts/build-backend-runtime.ts. Do not edit.
"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var agent_capability_exports = {};
__export(agent_capability_exports, {
  FARMING_COMPUTER_MCP_SERVER_NAME: () => FARMING_COMPUTER_MCP_SERVER_NAME,
  isFarmingComputerServer: () => isFarmingComputerServer,
  mergeComputerMcpServer: () => mergeComputerMcpServer
});
module.exports = __toCommonJS(agent_capability_exports);
const path = require("path");
const FARMING_COMPUTER_MCP_SERVER_NAME = "farming-computer";
const FARMING_COMPUTER_MCP_META_KEY = "farming.dev/extension";
function recordValue(value) {
  return value && typeof value === "object" ? value : {};
}
function isFarmingComputerServer(server) {
  const value = recordValue(server);
  return recordValue(value._meta)[FARMING_COMPUTER_MCP_META_KEY] === "computer";
}
function computerMcpEnv(agentEnv) {
  return [
    "FARMING_AGENT_ID",
    "FARMING_CONFIG_DIR",
    "FARMING_CONTROL_URL",
    "FARMING_DISABLE_AUTH",
    "FARMING_PROJECT_WORKSPACE",
    "FARMING_TOKEN_FILE"
  ].flatMap((name) => agentEnv[name] === void 0 || agentEnv[name] === "" ? [] : [{ name, value: String(agentEnv[name]) }]);
}
function mergeComputerMcpServer(mcpServers, options) {
  const existing = Array.isArray(mcpServers) ? mcpServers : [];
  const collision = existing.find((server) => recordValue(server).name === FARMING_COMPUTER_MCP_SERVER_NAME && !isFarmingComputerServer(server));
  if (collision) {
    throw new Error(
      `MCP server name "${FARMING_COMPUTER_MCP_SERVER_NAME}" is reserved by the Farming Computer Extension`
    );
  }
  const command = path.join(options.cliBinDir, process.platform === "win32" ? "farming.cmd" : "farming");
  return [
    ...existing.filter((server) => !isFarmingComputerServer(server)),
    {
      name: FARMING_COMPUTER_MCP_SERVER_NAME,
      command,
      args: ["computer", "mcp"],
      env: computerMcpEnv(options.agentEnv || {}),
      _meta: { [FARMING_COMPUTER_MCP_META_KEY]: "computer" }
    }
  ];
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  FARMING_COMPUTER_MCP_SERVER_NAME,
  isFarmingComputerServer,
  mergeComputerMcpServer
});
