const { BrowserResourceManager } = require('./browser-resource-manager');
const { createBrowserRouter } = require('./browser-router');

module.exports = {
  BrowserResourceManager,
  createBrowserRouter,
};
